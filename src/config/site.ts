import { SiteConfig } from '@/types';

export const siteConfig: SiteConfig = {
  navItems: [
    {
      label: 'home',
      href: '/',
    },
    {
      label: 'posts',
      href: '/posts',
    },
    {
      label: 'about',
      href: '/about',
    },
  ],
};
